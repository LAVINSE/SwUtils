namespace SWUtils
{
    /// <summary>
    /// SaveData, PlayerPrefs, Cloud가 함께 사용하는 저장 슬롯 이름 모음.
    /// </summary>
    public static class SWSaveSlot
    {
        /// <summary>
        /// 별도 슬롯 이름을 넘기지 않았을 때 사용하는 기본 슬롯.
        /// </summary>
        public const string Default = "SWUtils_Save_Slot";
    }
}
